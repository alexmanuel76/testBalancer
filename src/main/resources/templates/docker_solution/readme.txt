-create a MySQL database using the dump.sql file or create another, only need to match DB name and balancer table structure

-create a Docker image using the docker file as an example with the latest build JAR

-use the docker-compose.yml to create as server API as desired, check in the ENV the IP and user for the database